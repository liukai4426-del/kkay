# KAYTRADE V1.6.6 Build1661

本次为 V1.6.6 运行稳定性热修，不改变交易策略、评分、仓位、止损止盈或订单写入语义。

## 修复

- 瞬时 GET / 只读请求 `NetworkError` 不再全局停止自动交易。
- 只读请求失败时仅终止当前运行周期，保留自动交易授权；下一周期自动重新读取最新状态。
- 当前周期因数据不完整不会提交新订单；下一周期仍必须重新通过最新指标与 Final Entry Guard 才允许下单。
- 只读失败不再把 `network_paused` 置为全局暂停，不要求网络恢复后重新人工授权。
- 自动交易保持开启时 Mac 防睡眠保持启用。

## 强保护保持不变

- POST / 下单 / 撤单等写请求不会自动重试。
- 写入结果未知仍保持 fail-closed，禁止重复提交并要求核对交易所状态。
- 本地故障锁、活动订单/仓位保护、信号新鲜度、4H/RSI/MACD/Volume/结构/成本等 Final Entry Guard 不变。

## 适用场景

例如 `Algo OCO`、账户读取、行情等 GET 请求发生连接超时：程序记录只读告警并跳过当前周期，但不会因为一次只读超时关闭自动新开仓。
