# KAYTRADE V1.5.4 Build 1542

本修复版在 V1.5.4 LIMIT 修复版基础上继续处理界面与 BOLL 执行逻辑，不改变版本号。

## 本次改动

- 恢复原来的圆角「运行记录」卡片作为唯一可见运行记录区域。
- 删除后来新增的旧式 Text「运行日志」显示与“全部”筛选栏，不再把历史 `events.log` 自动回放到运行记录卡片。
- 评分明细中明确标注：`【开仓必要】` / `【辅助评分】`；5m BOLL 为开仓必要项，其余现有加分项为辅助评分。
- 「固定执行成本」卡片去掉 Canvas 黑色矩形外框/黑色沟槽，使其与执行参数页面背景自然衔接。
- 取消 5m BOLL 触发后的 1 分钟墙钟开仓截止。有效机会由趋势、结构、追价、成本等策略条件决定是否继续有效，不再因经过 60 秒自动失效。
- 仍使用 LIMIT 限价开仓；单次未成交限价委托继续沿用 60 秒安全订单生命周期，超时后撤单/核对，但只要 BOLL 机会仍有效，后续 1m 可重新评估。
- 1m 仍只负责执行节奏，不新增 EMA/KDJ/RSI/MACD 技术确认。
- 保留原有 1H ATR 止损、整仓 2R、前方 1.5R、成本 0.30R、30 分钟平仓冷却、连续 3 次净亏损暂停 1 小时、故障锁与不确定写入 fail-closed 机制。

## Build

- Display version: `1.5.4`
- Internal build: `1542`
- Intel Mac target: `x86_64`
- Minimum macOS: `14.0`
