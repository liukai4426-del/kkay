# OKX Local 1.1 · Intel Mac 测试安装包

新增：RSI极值评分自动交易、只读重连、深色评分控制台、历史收益与胜率。详见随包 `RELEASE-1.1.md`。
升级时退出旧App再替换。风险设置改用独立的 `settings-v1.1.json`，默认评分7、止损0.8×1H ATR、止盈1.5R；旧订单/故障锁/历史记录保留，不要删除它们。

适用目标：Intel Mac、macOS 14 及更新版本。已打包 Python、Tk 和 HTTPS 根证书，不需要你另外安装 Python，不使用爬虫或云端交易服务器。

## 安装

打开 DMG，将 OKXLocal.app 拖入 Applications（应用程序），然后从应用程序打开。
此测试包没有 Apple Developer ID 签名和公证，macOS 可能阻止首次打开。核实来源后可在系统设置的“隐私与安全性”中处理打开提示；不要关闭系统安全保护。

## 首次使用

默认停止，默认 OKX 模拟盘。先在本机填写模拟盘 API Key、Secret、Passphrase 并测试只读连接。密钥只在运行内存中保存，退出后重新填写，不要上传到 GitHub 或聊天。

模拟盘逐项验收：逐仓、方向、杠杆、合约张数、成交回报、止盈止损在交易所生效和触发平仓、停止新开仓、重启不恢复自动开仓。详细参数见 README-中文.md（仓库内）。

本安装包保留真实账户选项，默认未启用。切换真实账户需要重新连接及输入 LIVE 确认。没有在你的账户上完成端到端交易验收，不要把程序测试通过当作实盘已验证。

## 测试边界

构建流程在 GitHub 的 Intel macOS 15 上运行模拟接口测试、打包后窗口与设置检查、Intel 二进制及 macOS 最低版本检查。它没有验证你的 macOS 14 实机、OKX 网络连通性、账户资格、模拟盘/实盘成交和止损触发。

Mac 关机、睡眠或断网后不会继续运行策略。只有已被 OKX 接受并生效的保护单才不依赖本机在线。
