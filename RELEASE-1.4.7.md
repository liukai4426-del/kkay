# KAYTRADE 1.4.7

本版本在 V1.4.6 的界面、下单、仓位与安全机制基础上，仅更新交易信号评分与开仓硬过滤。

## 策略更新

- 删除全部 1D 日线指标，不再请求 `1Dutc` K线，不参与评分、过滤或趋势判断。
- 前方有效结构空间 `< 1.3R` 时直接禁止开仓/加仓；`>= 1.3R` 不再扣分，也不再警告。
- 1H 与开仓方向明确顺势时 `+2`；明确逆势时直接禁止该方向开仓/加仓；中性为 `0`。
- 4H 与开仓方向明确顺势时 `+1`；明确逆势继续按原规则 `-1`；中性为 `0`。
- 1m EMA 短期方向取消加分，也不能单独激活 1m Trigger。
- 有效 1m Trigger 仅由 EMA20 回收 / KDJ 交叉 / 反转K线构成，每项 `+0.5`，模块最高 `+1`，且 Trigger 必须大于 0。
- 5m MACD 与开仓方向一致成为硬性开仓条件，同时 `+1`；5m BOLL 方向仍保留 `+1`。
- 15m Setup、15m 趋势、RSI惩罚、1H/4H不利结构、V1.4 多空评分仲裁继续保留。

## 保持不变

- 自动开仓最低评分：`4.0/10`。
- Tier 1：4.0–6.0；Tier 2：6.5–7.5；Tier 3：8.0–10.0。
- 15m ATR 止损，整仓 `2R` 止盈。
- 开仓限价、TP/SL trigger-limit、手动/安全平仓市价。
- 每根已收盘1m最多一次新开仓/加仓，60秒限价有效期，0.3×1H ATR追价保护。
- 同Tier每周期最多一次、只允许同方向加仓、持仓保护确认、冷却、连续亏损、日内风险、故障锁与Fail-closed机制。

> 本版本为策略规则实现与测试版本，不代表未来收益。