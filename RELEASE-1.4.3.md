# KAYTRADE V1.4.3

## 新增：多空信号评分仲裁

- 只有已经分别通过原有评分门槛、1m Trigger、结构空间及其他入场硬条件的方向，才进入比较。
- 仅一侧合格时，按原逻辑选择该方向。
- 多空同时合格时，只允许最终总评分更高的一侧开仓，不会同时开多和开空。
- 多空最终总评分相同时，本轮不提交任何开仓订单，保持自动交易运行并进入方向等待。
- 等待期间按后续已收盘 1m 信号重新计算；某一侧分数变高且仍满足全部条件后，只允许该侧开仓。
- 未改变 V1.4.2 的评分组成、4.0 固定门槛、仓位档位、TP/SL、手续费过滤、下单防重与故障锁。

## 构建

- macOS 14+，Intel x86_64。
- 版本号：1.4.3（Bundle 143）。
