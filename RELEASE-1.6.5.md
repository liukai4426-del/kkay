# KAYTRADE V1.6.5 · Build1650

## 核心更新

- 所有技术指标只使用各自周期最新的**已收盘K线**，不使用未收盘K线。
- 5m BOLL外轨信号及同根5m RSI / Volume / KDJ 信号属性，有效至**下一根5m K线收盘**。
- 新5m K线收盘后，上一组5m信号立即失效并重新计算；满足外轨时 BOLL=2.5 分，不满足则归0。
- 15m / 1H / 4H 在各自新K线收盘后自动刷新。
- Final Entry Guard 在真实开仓前重新检查 1m / 5m / 15m / 1H / 4H 周期新鲜度；如刚跨过新收盘边界则取消本轮，先刷新再判断。
- Volume 不计分；5m信号K Volume >= 前20根5m均量1.20× 时 Hard Gate 禁止开仓。
- 仅5m BOLL上下外轨；5m RSI 30–70、5m MACD连续改善、4H同向为必要条件。
- 评分门槛固定 >=6；前方强结构 >=1.5R、成本 <=0.30R、止损结构和入场偏离限制继续保留。
- 首次开仓固定 1× LIMIT；止损=1×1H ATR；整仓2R止盈；No-BE。

## UI / 授权确认

- 自动交易确认弹窗只显示 **V1.6.5 当前实际生效策略**。
- 删除 1D EMA、旧1m Trigger、中轨路径、历史仓位倍率等旧版本说明。
- 删除输入 DEMO / LIVE 的授权方式。
- 弹窗只保留 **确认 / 取消** 两个按钮，并使用与主程序一致的深色圆角UI。
- 评分明细、开仓状态、启动日志、版本副标题同步更新到 V1.6.5 口径。

## 数据安全

- OKX K线如未按周期及时确认收盘，继续 fail-closed，不使用旧K线补开仓。
- 保留 V1.6.4 的 LIMIT 写入前安全校验、逐仓杠杆回读、账户仓位/挂单检查、时钟同步与网络写入幂等保护。
