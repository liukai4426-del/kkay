# 1.2.2 K线与故障锁稳定性修复

本版本不修改 V1.2 的视觉方向，也不修改既有交易策略、评分阈值、仓位模式、止盈止损或下单规则。

## 修复内容

- 将“最新已收盘K线晚到”与“K线结构/价格/时间数据异常”分开处理。
- 收盘后的正常结算等待仍最多 45 秒，期间只等待，不使用旧信号开仓。
- 超过等待窗口后，K线晚到按连续异常计数：第 1、2 次只跳过当轮；连续第 3 次停止自动新开仓。
- 连续K线晚到导致的暂停不写入永久故障锁。行情恢复后恢复观察，但如果已经因连续 3 次晚到停止自动交易，需要重新授权启动，不会自动恢复下单。
- K线字段缺失、价格非法、时间对齐异常、历史缺口等数据完整性问题仍按永久故障锁处理。
- 修复“已有故障锁：已有故障锁：……”重复叠加问题。旧状态文件中的重复前缀/后缀会自动整理为单一原始原因，但不会自动解除真正的故障锁。
- K线日志继续显示 1H / 15m 周期、最新收盘时间、交易所校准时间和落后秒数。

## 升级后的旧故障锁

如果 1.2.1 已经把“ K线已过期，暂停策略判断 ”写入本地故障锁，1.2.2 会先把重复文字清理干净，但不会直接替你解除该旧锁。首次运行 1.2.2 后，请先测试连接并在 OKX 核对 BTC 仓位与挂单；确认无异常后点击“核对后解除故障锁”，再重新授权启动自动交易。

## 安全行为保持不变

订单状态不确定、仓位与本地记录不一致、TP/SL 保护无法核实、账户/API权限异常等需要人工核对的故障仍会永久锁住新开仓。POST 写入请求仍不自动重试。

构建目标为 Intel macOS 14+ 测试版。CI 使用假交易所和合成K线验证，不连接用户账户、不提交真实订单；仍未完成用户 Mac 与 OKX 账户端到端实盘验收，也未做 Apple Developer ID 签名与公证。
