# KAYTRADE V1.6.3 · Build 1630

## Strategy
- 关闭 5m BOLL 中轨开仓路径；信号源仅保留外轨。
- 做多：5m BOLL 下轨触发；做空：5m BOLL 上轨触发。
- 多空统一要求 5m RSI 位于 30–70。
- 外轨统一要求 5m MACD Histogram 连续向交易方向改善。
- 继续要求 4H 与交易方向同向。
- 继续沿用 V1.6.2 的 1× LIMIT 首仓、1×1H ATR 止损、2R 整仓止盈、No-BE，以及原有结构/成本 Hard Gate。

## OKX 51290 修复
- 新发生的 OKX 51290 只暂停新开仓，不写入永久故障锁。
- 退避时间继续采用 5s → 10s → 15s → 30s，满足恢复条件后自动恢复。
- 启动/连接时若发现旧版本遗留的 51290 故障锁，会先只读核对 BTC 仓位、普通委托、策略委托、关联订单及历史订单。
- 只有在确认没有成交/未完成订单风险时才自动清除旧锁；存在风险证据时继续保留故障锁，禁止重复下单。

## UI
- 顶部 BTC / USDT 副标题同步显示 `V1.6.3`，不再残留 `V1.5.4 UI精修版`。
- 运行记录滚动条滑块改为最大圆角胶囊形。
- 开仓状态栏同步到外轨模型：外轨 BOLL / 外轨 MACD / RSI 30–70 / 外轨 4H。

## Packaging
- Intel Mac x86_64 / macOS 14+。
- Bundle version: 1.6.3 / 1630。
