# 测试结果 — 2026-09-09

## 已完成

- Python源码编译检查。
- 36项自动测试通过：纯本地FakeExchange/假HTTP响应，无外部账户权限。
- 覆盖方向对称的TP/SL价格、名义仓位/风险预算、十进制张数取整、最小数量拒单、价差过滤、手续费预算。
- 覆盖逐仓+附带保护单的请求体、订单级sCode错误、签名及模拟盘请求头、禁止提币路由。
- 覆盖提交前落盘、网络结果未知、订单去重、FOK取消、仓位不匹配、保护单缺失/数量不足。
- 覆盖每日亏损与连续亏损、停止不撤保护单、平仓方向、禁止重复平仓、睡眠停机、重启不自动运行、损坏状态文件拒绝启动。

## 未完成，不能宣称通过

- macOS Intel / Apple Silicon 真机安装、Tk窗口交互和布局验证。
- OKX公网连接与历史K线分页的端到端验证；开发环境网络受限。
- OKX模拟盘实际开户、下单及保护单字段回读兼容性测试。
- OKX实盘下单/成交/止损止盈触发/极端行情验证；没有用户密钥，也没有发送订单。
- 收益回测、胜率和盈利能力验证。
- 打包为已签名、公证、内置Python运行时的 `.app` / `.dmg`。

本包为待端到端验收的可运行源码测试版，不是经过生产验证的交易终端。

## 用户侧验收清单

1. 启动窗口成功；密钥字段遮挡，退出后不留存。
2. 用模拟盘密钥测试连接，不应出现任何新订单。
3. 保存参数、再次启动，参数保持而实盘/自动开关不恢复。
4. 模拟盘出现信号时核对逐仓、方向、张数、FOK状态。
5. 成交后在OKX核对TP/SL是否已生效；本地需显示核对成功，不只是提交成功。
6. 核对TP/SL触发后实际仓位归零；暂停/恢复不会同一K线重复开仓。
7. 测试网络失败、电脑睡眠、重复启动、止损保护单缺失时停止新开仓。
8. 日回撤/连续亏损达到设置阈值时停止；核对后解除故障锁不能清空风控基准。

任何项失败时不要切换实盘；保留脱敏错误码及事件记录排查。
