name: Build KAYTRADE Intel Mac 1.6.8 Build1681

on:
  push:
    branches: [fix/v168-build1681-runtime-log-sync]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  unit:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Install runtime test dependencies
        run: python -m pip install 'websocket-client==1.8.0' 'certifi==2025.8.3'
      - name: Compile Build1681 runtime
        run: |
          python -m py_compile \
            v160_model.py v160_update_patch.py v161_model.py v161_update_patch.py v162_model.py v162_update_patch.py \
            v163_model.py v163_update_patch.py v163_ui_patch.py v164_model.py v164_update_patch.py v164_ui_patch.py \
            v165_model.py v165_update_patch.py v165_algo_fallback_patch.py v165_runtime_network_patch.py \
            v165_sleep_resume_patch.py v165_keepawake_patch.py v165_ui_patch.py v165_15m_outer_patch.py \
            v166_ui_patch.py v166_readonly_resilience_patch.py \
            v167_strategy_patch.py v167_algo_sync_patch.py v167_15m_boll_only_fix.py \
            v168_update_patch.py v168_build1681_patch.py test_v168_update.py test_v168_build1681.py desktop.py
      - name: Preserved strategy and runtime regressions
        run: |
          python -m unittest -v test_engine
          python -m unittest -v test_candles
          python -m unittest -v test_v165_update
          python -m unittest -v test_v165_execution
          python -m unittest -v test_v165_runtime_network_state
          python -m unittest -v test_v165_sleep_resume
          python -m unittest -v test_v165_keepawake
          python -m unittest -v test_v165_ui
          python -m unittest -v test_v166_ui
          python -m unittest -v test_v166_readonly_resilience
          python -m unittest -v test_v167_strategy
          python -m unittest -v test_v167_algo_sync
          python -m unittest -v test_v167_15m_boll_only_fix
          python -m unittest -v test_v168_update
          python -m unittest -v test_v168_build1681
      - name: Verify Build1681 semantic lock
        run: |
          python - <<'PY'
          import re
          from pathlib import Path
          import app
          import v153_model as signal_core
          import v165_model as model
          import v167_15m_boll_only_fix as b1671
          import v168_update_patch as v168
          import v168_build1681_patch as b1681
          spec=Path('OKXLocal.spec').read_text()
          desktop=Path('desktop.py').read_text()
          assert b1681.VERSION=='1.6.8' and b1681.BUILD=='1681'
          assert v168.VERSION=='1.6.8' and v168.BUILD=='1681'
          assert model.VERSION=='1.6.8' and model.BUILD=='1681'
          assert model.THRESHOLD==6.0
          assert model.ENTRY_WINDOW_MS==900000
          assert model.BOLL_OUTER_SCORE==2.0
          assert model.BOLL_TRIGGER_TIMEFRAME=='15m'
          assert model.FIVE_MINUTE_BOLL_ENABLED is False
          assert signal_core.boll_entry_signal is b1671._signal_adapter_15m_only
          assert getattr(app.App,'_kaytrade_v168_build1681_applied',False)
          sample=b1681._sanitize_runtime_text('V1.5.4未开仓：BOLL信号执行窗口·第5个1m；计划市价参考；V1.6.2要求4H同向：5m BOLL中轨/外轨在4H中性或逆向时禁止开仓；V1.6.6 Volume Hard Gate：5m信号K量能 1.89× ≥ 1.20×，禁止开仓')
          assert 'V1.5.4' not in sample and 'V1.6.2' not in sample and 'V1.6.6' not in sample
          assert re.search(r'(?<!1)5m BOLL', sample) is None
          assert '中轨' not in sample and '计划市价参考' not in sample
          assert '15m BOLL外轨' in sample and '4H Hard Gate' in sample and '计划限价' in sample and '5m信号K量能 1.89×' in sample
          sync=b1681._sanitize_runtime_text('只读查询暂时失败 /ws/v5/business:orders-algo：Algo实时状态暂未完成可信同步')
          assert sync.startswith('Algo状态同步中：') and '只读查询暂时失败' not in sync
          assert "CFBundleShortVersionString': '1.6.8'" in spec
          assert "CFBundleVersion': '1681'" in spec
          assert 'v168_build1681_patch' in desktop
          print('PASS Build1681: strategy unchanged; stale runtime wording and Algo sync presentation fixed')
          PY

  build:
    needs: unit
    runs-on: macos-15-intel
    timeout-minutes: 70
    env:
      MACOSX_DEPLOYMENT_TARGET: '14.0'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          architecture: x64
      - run: python -m pip install 'pyinstaller==6.16.0' 'certifi==2025.8.3' 'Pillow==11.3.0' 'websocket-client==1.8.0'
      - name: Build1681 regression tests
        run: |
          python -m unittest -v test_v167_algo_sync
          python -m unittest -v test_v167_15m_boll_only_fix
          python -m unittest -v test_v168_update
          python -m unittest -v test_v168_build1681
      - run: python build_icon.py
      - run: python -m PyInstaller --noconfirm OKXLocal.spec
      - name: Verify Intel bundle and metadata
        run: |
          python verify_bundle.py
          /usr/bin/plutil -p dist/KAYTRADE.app/Contents/Info.plist | grep '1.6.8'
          /usr/bin/plutil -p dist/KAYTRADE.app/Contents/Info.plist | grep '1681'
          codesign --verify --deep --strict dist/KAYTRADE.app
      - name: Packaged Build1681 smoke test
        run: |
          TMPDIR_CHECK="$(mktemp -d)"
          dist/KAYTRADE.app/Contents/MacOS/KAYTRADE --smoke-test "$TMPDIR_CHECK/smoke.txt"
          cat "$TMPDIR_CHECK/smoke.txt"
          grep 'V1.6.8 Build1681' "$TMPDIR_CHECK/smoke.txt"
          grep 'BOLL remains strict closed-15m outer-only' "$TMPDIR_CHECK/smoke.txt"
          grep 'legacy 5m-BOLL/middle-band and MARKET-entry text is sanitized' "$TMPDIR_CHECK/smoke.txt"
          grep 'Algo cache resynchronization is shown as sync-in-progress' "$TMPDIR_CHECK/smoke.txt"
          grep 'write ambiguity remains fail-closed' "$TMPDIR_CHECK/smoke.txt"
      - name: Create installer
        run: |
          mkdir -p installer
          ditto dist/KAYTRADE.app installer/KAYTRADE.app
          printf 'KAYTRADE V1.6.8 Build1681\nRuntime/log cleanup only. Trading strategy is unchanged from Build1680. Legacy 5m BOLL middle/outer wording is removed; BOLL remains latest closed 15m outer-only. Expected Algo cache resynchronization is displayed as sync-in-progress instead of a false read-query failure. Legitimate 5m RSI/MACD/Volume confirmations and all write-safety protections remain.\n' > installer/RELEASE-1.6.8-B1681.txt
          hdiutil create -volname 'KAYTRADE 1.6.8 Build1681' -srcfolder installer -ov -format UDZO KAYTRADE-1.6.8-Intel-build1681.dmg
      - uses: actions/upload-artifact@v4
        with:
          name: KAYTRADE-1.6.8-Intel-build1681
          path: |
            KAYTRADE-1.6.8-Intel-build1681.dmg
            installer/RELEASE-1.6.8-B1681.txt
          if-no-files-found: error
