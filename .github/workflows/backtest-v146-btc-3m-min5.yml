name: Backtest KAYTRADE V1.4.6 BTC SWAP 3M MIN5
on:
  push:
    branches: [research/v146-btc-3m-backtest]
    paths:
      - research_v146_backtest.py
      - research_v146_backtest_min5.py
      - .github/workflows/backtest-v146-btc-3m-min5.yml
  workflow_dispatch:
permissions:
  contents: read
jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Compile min-score-5 research backtest
        run: python -m py_compile research_v146_backtest.py research_v146_backtest_min5.py
      - name: Run V1.4.6 BTC-USDT-SWAP min-score-5 three-month backtest
        env:
          BACKTEST_OUT: backtest_output_min5
        run: python research_v146_backtest_min5.py
      - name: Show report summary
        if: success()
        run: |
          cat backtest_output_min5/metrics.json
          echo '--- REPORT ---'
          cat backtest_output_min5/V146_BTC_SWAP_3M_MIN5_Backtest_Report.md
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: KAYTRADE-V146-BTC-SWAP-3M-MIN5-BACKTEST
          path: |
            backtest_output_min5/V146_BTC_SWAP_3M_MIN5_Backtest_Report.md
            backtest_output_min5/V146_BTC_SWAP_3M_Trades.csv
            backtest_output_min5/V146_BTC_SWAP_3M_Cycles.csv
            backtest_output_min5/V146_BTC_SWAP_3M_DailyEquity.csv
            backtest_output_min5/metrics.json
          if-no-files-found: error
          retention-days: 14
