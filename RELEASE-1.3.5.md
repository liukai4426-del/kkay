# KAYTRADE V1.3.5

本版基于 V1.3.4 保留原有策略结构，并补充执行与安全修复。

- 自动开仓评分阈值改为可选 `1—10`，以 `0.5` 为步进；总评分仍为 10 分制。
- 每次点击“启动自动交易”后增加 5 秒启动缓冲；缓冲期间只刷新行情/核对仓位，不允许发送新的自动开仓订单。
- 取消原“单笔风险不得超过配置资金5%”硬上限。`risk_usdt` 与 `risk_pct` 可自由设置为有限正数；评分 1× / 1.5× / 2× 放大后仍受日内剩余亏损预算、最大名义仓位、杠杆和可用余额约束。
- 修复下单后 OKX 51603。成功下单后持久化 OKX `ordId`，后续优先使用 `ordId` 查询/撤单。
- 51603 不再被误报为权限/地区/系统时间问题；会先核对当前委托、近期历史订单和 BTC 持仓，期间禁止重复下单。
- 新增 OKX `50123` 明确拒单处理：记录请求方法与接口；当交易所明确拒绝开仓 POST 时，立即判定本次订单未创建，清理提交前的本地 `active` 占位记录，停止新开仓，并且不再进入后续 51603 轮询。
- 若发现匹配持仓，继续按已成交仓位检查 TP1/TP2/SL；若旧版残留记录经多路核对确认无订单、无成交、无持仓，则安全清理；若 OKX 已确认过 `ordId` 但状态长期矛盾，继续 fail-closed 停止新开仓并要求人工核对。
- 日内权益回撤达到上限后改为“中国时间当日停止新开仓”，不再写入可反复解除/重触发的永久故障锁；日志显示峰值权益、当前权益、已回撤与上限，次日自动重建基准。
- 15m刚收盘后的正常OKX确认窗口延长至60秒，仅显示等待状态，不再每15分钟写入故障式日志；持续过期仍会安全暂停旧信号。
- 保留日线 EMA 评分、1H/4H 趋势扣分、分级仓位、逐仓双向、15m ATR 止损、TP1=1R 50% → 保本 → TP2=2R 50%。

测试版仍不保证盈利，真实资金使用前请先在 OKX 模拟盘验证完整下单与保护单流程。若出现 `50123`，程序会明确记录被拒绝的接口并避免进入无意义的 51603 轮询；仍需根据OKX返回信息核对API交易权限、账户环境和产品资格后，订单才能被接受。

构建状态：2026-09-11 仓库公开后重新触发 Intel macOS 14 安装包验证。
