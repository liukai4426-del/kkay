# KAYTRADE V1.6.4 · Build 1640

V1.6.4 基于正式版 `release/v1.6.3`，只调整已经确认的评分 / Hard Gate / UI 语义，保留既有下单生命周期、51290 安全机制与 V1.6.3 外轨规则。

## 本次更新

- 删除 **5m Volume +0.5** 评分。
- 原 Volume 的 **+0.5 转移到 5m BOLL 外轨触发**：
  - 做多：5m BOLL 下轨；
  - 做空：5m BOLL 上轨；
  - 外轨触发评分由 2.0 调整为 **2.5**。
- 新增 **Volume Hard Gate**：
  - 量能定义：当前 5m 信号K成交量 ÷ 前20根 5m 平均成交量；
  - **Volume ≥ 1.20×：禁止新开仓**；
  - **Volume < 1.20×：量能门槛允许开仓**；
  - Volume 不再参与总评分。
- 评分明细删除 Volume 项，删除旧的“中轨”文字，并按当前外轨执行条件校准 BOLL / MACD / 4H 文案。
- “开仓状态”删除 **可用资金**，增加 **量能 Hard Gate**。
- 开仓状态颜色统一：
  - **允许 / 条件通过：绿色**；
  - **禁止 / 条件未通过：红色**；
  - 暂无可判定数据：灰色。
- 量能状态会直接显示：
  - `量能：允许开仓`（绿色）；
  - `量能：禁止开仓`（红色）。

## 保留不变

- 仅 5m BOLL 外轨开仓；
- 5m RSI 必须处于 30–70；
- 5m MACD 柱必须连续向交易方向改善；
- 4H 必须同向；
- 首次开仓固定 1×；
- LIMIT 开仓；
- 1× 1H ATR 止损；
- 2R 整仓止盈；
- No-BE；
- OKX 51290 继续使用 V1.6.3 的软暂停与历史锁安全迁移机制。
