# KAYTRADE V1.6.4 · Build 1641

V1.6.4 基于正式版 `release/v1.6.3`。Build1641 在保留已确认评分 / Hard Gate / 外轨规则的同时，完成一次入场执行链修复与端到端检查。

## 本次策略与评分

- 删除 **5m Volume +0.5** 评分。
- 原 Volume 的 **+0.5 转移到 5m BOLL 外轨信号**：
  - 做多：5m BOLL 下轨；
  - 做空：5m BOLL 上轨；
  - 外轨评分由 2.0 调整为 **2.5**。
- **Volume Hard Gate**：
  - 量能定义：当前 5m 信号K成交量 ÷ 前20根 5m 平均成交量；
  - **Volume ≥ 1.20×：禁止新开仓**；
  - **Volume < 1.20×：量能门槛通过**；
  - Volume 不再参与总评分。

## Build1641 执行修复

- **5m BOLL 外轨信号只在该信号K收盘后的 4 分钟内有效**。
- 到达第 4 分钟边界立即失效，必须等待新的已收盘 5m 外轨信号。
- 程序启动时会清理旧版本遗留的过期 `opportunity`，禁止旧外轨信号跨重启继续参与开仓。
- 修复旧 LIMIT submitter 与 `ENTRY_WINDOW_MS=0` 的冲突，正式下单器统一使用 4 分钟窗口。
- 新增 **Final Entry Guard**：进入下单器前再次确认评分 / Hard Gate / RSI / MACD / 4H / Volume / 信号窗口 / 时钟状态。
- 杠杆读取或网络步骤耗时后，LIMIT 写入 OKX 前会再次按服务器时间检查 4 分钟窗口；若已超时，不提交订单。
- 保留实际限价的二次检查：入场偏离、前方结构空间、1H ATR 止损结构、成本预算等仍必须通过。
- 新增真实下单链单元测试，覆盖：
  - 有效信号只能 POST 一次；
  - 4 分钟过期不得 POST；
  - Volume ≥1.20× 不得 POST；
  - 杠杆预检期间信号过期不得 POST；
  - 启动时过期信号必须被清理。

## UI 校准

- 评分明细删除 Volume 项及旧“中轨”文字。
- BOLL 文案改为 **「5m BOLL外轨信号（4分钟有效）」**，避免误解为当前价格仍位于外轨。
- “开仓状态”删除 **可用资金**。
- 增加 **外轨信号≤4m** 状态。
- Volume 仅出现在“开仓状态”的 Hard Gate。
- 状态颜色统一：
  - **允许 / 条件通过：绿色**；
  - **禁止 / 条件未通过：红色**；
  - 暂无可判定数据：灰色。

## 保留不变

- 仅 5m BOLL 外轨开仓；
- 5m RSI 必须处于 30–70；
- 5m MACD 柱必须连续向交易方向改善；
- 4H 必须同向；
- 固定评分门槛 6.0；
- 首次开仓固定 1×；
- LIMIT 开仓；
- 1× 1H ATR 止损；
- 2R 整仓止盈；
- No-BE；
- Path C / 加仓关闭；
- 连续亏损暂停关闭；
- 平仓后冷却关闭；
- OKX 51290 继续使用 V1.6.3 的软暂停与历史锁安全迁移机制。
