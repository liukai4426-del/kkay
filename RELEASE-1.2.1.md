# 1.2.1 K线过期修复

交易规则、评分阈值和仓位模式不变。首次预热历史数据后，按需更新最近K线并缓存；同一已收盘周期不重复下载。

K线、最新成交价和API认证使用交易所校准时间，校准后以单调时钟推进，并定期重新校准。每次执行前重新核对K线周期。

收盘后最多等待45秒让新K线就绪，期间不使用旧信号开仓。新数据到达后，已授权且无其他故障的策略继续判断。超过窗口、数据缺口、时间异常仍锁定停止新开仓。日志列出周期、最新收盘时间及落后秒数。

退出旧App，拖入Applications替换后启动，标题为1.2.1。无需清空原设置或账户记录。旧故障锁仍保留；请先测试连接并核对账户，再解除原故障锁及启动。不会因安装更新自动开启交易。

验证使用假交易所、时钟与合成K线，未连接用户账户或提交订单。Intel macOS 15构建并检查最低macOS14目标；用户Mac及真实账户端到端验证仍未完成。未做Apple Developer ID签名与公证。
