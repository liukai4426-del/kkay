# KAYTRADE V1.5.5

基线：V1.5.4 Build1544。交易模型本身保持不变：Path C 继续关闭；第一信号 LIMIT 开仓；评分门槛 6.0；Hard Block、1×1H ATR 止损、2R 整仓止盈均保持。

本次更新：

1. 修复账户连接完成后风险设置 / 执行参数输入框可能无法继续编辑的问题。仅当自动交易正在运行时锁定可编辑设置；停止自动交易后恢复编辑。
2. 运行记录每条日志完整显示 `YYYY-MM-DD HH:MM:SS` 时间戳。
3. 运行记录支持鼠标滚轮 / 触控板滚动，并保留右侧滚动条；用户正在查看历史日志时，新日志不会强制拉回底部。
4. 风险设置删除“第二信号仓位”整行，只保留第一信号仓位。
5. 第一信号仓位重新定义为“逐仓保证金”。
6. “策略资金预算”重新定义为杠杆后的名义仓位金额，自动计算：`第一信号保证金 × 逐仓杠杆`，只读不可手动修改。
7. Path C 仍然关闭。隐藏兼容设置中的第二信号值改为 `第一信号保证金 × 3`，不参与新开仓、不占用风险预算。
8. 连接/保存等结果弹窗销毁后主动清理残留 Tk grab，避免 macOS Aqua 下出现界面可见但输入框失去焦点的状态。

版本：1.5.5
内部 Build：1550
目标：Intel Mac / macOS 14+
