# KAYTRADE V1.6.1 · Build 1610

本版本基于 V1.6.0 正式策略，仅加入已确认的三项调整；其余评分、仓位、成本与基础风控保持 V1.6.0 语义。

## 1. 时钟同步容错与自动恢复

- OKX Server Time 改为 3 次采样，以最低 RTT 样本建立服务器时间锚点，并检查样本间 offset 稳定性。
- 网络 RTT 与真实时间偏差分开记录，不再因为单次 `时钟同步延迟过高` 就写入永久故障锁。
- 时钟同步暂时不稳定时，只暂停**新的自动开仓**；已有仓位、TP/SL、平仓与账户核对继续运行。
- 按 5 / 10 / 15 秒退避重试；连续 3 个样本恢复正常后自动恢复新开仓，无需重新授权。
- 运行日志显示 RTT、时间偏差、有效样本数与恢复状态。
- 修正旧日志中残留的 `V1.5.4未开仓` 版本文案。

## 2. 5m BOLL 外轨必须 4H 同向

- 5m BOLL **下轨做多**：只有 4H 趋势与做多方向明确同向时才允许开仓。
- 5m BOLL **上轨做空**：镜像规则，只有 4H 趋势与做空方向明确同向时才允许开仓。
- 4H 中性或逆向时，外轨信号直接 Hard Gate，不能由更高评分补偿。
- 中轨 `middle_rsi` 继续沿用 V1.6.0：正式 5m `macd_improving` 为必须项，中轨 1×；外轨仍为单张 LIMIT 2×。

## 3. +1R → BE

- 持仓价格首次达到 **+1.0R** 时，程序对现有 OKX 策略止损执行一次改单，将 SL 移到交易所当前持仓的实际平均开仓价。
- 做多/做空完全镜像。
- BE 不加入手续费补偿，严格使用开仓均价（按合约 tick 向安全方向取整）。
- 原始整仓 2R 止盈保持不变。
- 改单请求不会盲目重试：网络结果不确定时保留原始 SL，并通过只读策略委托状态核对；确认 OKX 已更新后才同步本地保护参数。

## 保持不变

- 固定开仓阈值：6.0 / 10。
- 中轨 1×基础固定仓位；BOLL 外轨 2×基础固定仓位，均为单张 LIMIT 首仓。
- 初始止损：1× 1H ATR。
- 最终止盈：整仓一次性 2R。
- Path C 关闭。
- 平仓后冷却关闭。
- 连续亏损暂停开仓保持 V1.6.0 当前配置语义。
