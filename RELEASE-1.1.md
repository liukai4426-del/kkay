# OKX Local 1.1.0 测试安装包

目标：Intel Mac，macOS 14+。内置 Python/Tk/CA 证书，无需另装 Python。未作 Apple Developer ID 签名或公证。

## 策略（已确认规则的精确定义）

全部以已收盘K线计算。RSI14（Wilder）、Bollinger20/2、EMA20、KDJ9/3/3、ATR14（Wilder）。
做空硬条件：15m RSI>=75 且1H RSI>=70；做多：15m RSI<=20 且1H RSI<=25。
门槛不满足仍展示参考分，但禁止开仓。

|项目|多头|空头|最高分|
|---|---|---|---|
|15m RSI|15<RSI<=20得1，RSI<=15得2|75<=RSI<80得1，RSI>=80得2|2|
|1H RSI|20<RSI<=25得1，RSI<=20得2|70<=RSI<75得1，RSI>=75得2|2|
|布林|15m收盘低于下轨|15m收盘高于上轨|1|
|KDJ|当前已收盘J<=0且本根K上穿D|当前已收盘J>=100且本根K下穿D|2|
|反转K线|15m阳线且收盘突破前一根高点|15m阴线且收盘跌破前一根低点|1|
|EMA偏离|EMA20-收盘>=0.7×15m ATR|收盘-EMA20>=0.7×15m ATR|1|
|EMA重新穿越|前一根收盘<=前EMA20且本根收盘>本EMA20|前一根收盘>=前EMA20且本根收盘<本EMA20|1|

所有项目只看同一根最新已收盘15m K线，EMA重穿比较前一根。没有将“之前曾超买/超卖”延长成多根有效期。
总权重为10，部分条件互斥，不保证单根能取满10分；阈值默认7，可设7—10整数。10分阈值可能使策略无法成交。没有半分、评分仓位加倍或基于分数切换止盈。信号严格，可能长时间观望。

两项RSI硬条件及总分达标后，还需：已授权自动交易、网络及账户正常、无策略仓位/外来挂单、未达亏损上限、冷却结束、数据新鲜、当前价偏离信号<=0.3×1H ATR。FOK限价单未成交则取消；每根15m信号最多尝试一次，不追价不加仓。所有仓位按用户设置逐仓杠杆。

默认止损距离0.8×1H ATR，止盈1.5R，以拟提交入场限价为参考并按tick取整。按风险USDT/比例/剩余日亏损预算的较小值计算数量，同时受最大名义仓位及余额约束。TP/SL请求附带不等于交易所已生效，程序须查询保护单核实。实际跳空与滑点可使损失超过预算。

## 网络恢复

瞬态DNS/TLS/超时与HTTP408/429/500/502/503/504只对GET重试，总共3次，间隔1/2秒。认证错误及证书校验错误不重试。POST只发一次，即使超时也不重发。
耗尽重试后停止新开仓并保留故障锁。每30秒探测，连续两轮公共接口、账户、余额、持仓和委托查询成功后，核对本程序活动订单，恢复观察。不会自动解锁或重新开仓；本轮K线标为跳过，不补发断线信号。已有TP/SL不会被撤销。公共/账户自检均不下单。

## 历史收益与胜率

按当前已连接账户及模拟/真实环境隔离，从该账户本地历史读取本程序完成的平仓轮次。相同客户端订单号去重；提交失败、FOK未成交、未平仓记录均不计入。胜率=权益变化大于0的轮次/全部已平仓轮次，持平包含在分母；无交易显示“—”。
收益=完整交易轮次前后USDT账户权益差，含费用/资金费和外部资金变化；不是逐成交核算收益。账户入出金、手动交易等会污染估算，因此仍要求专用账户。历史明细展示最近200条，汇总/曲线统计全部可读完成轮次；损坏行提示并跳过。旧记录方向缺失标为“旧记录”，不编造。

## 升级

停止新开仓并退出旧App，安装新的OKXLocal.app。保留 Application Support/OKXLocal 的账户记录。V1.1独立设置文件从保守默认值开始，重新设置预算和风险后再授权。不自动启动、不自动继承风险参数、不清零故障锁。

## 验收范围

本地自动化测试涵盖评分边界、门槛执行、去重、仓位/保护单核对、网络重试、POST不重发、HTTP脱敏、历史统计。GitHub Intel macOS15执行打包、macOS14最低版本检查、证书与窗口/表单/评分/历史渲染检查。尚未在用户macOS14实机完成此版本验收，也未使用用户密钥进行OKX模拟/实盘端到端订单验收。没有历史回测收益或盈利保证。
