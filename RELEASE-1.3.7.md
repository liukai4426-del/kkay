# KAYTRADE V1.3.7

本版仅调整本轮确认的策略与执行界面，不改变 V1.3.6 的网络写入去重、故障锁、实时账户权益、成本诊断与 5 秒启动缓冲。

## 策略变更

- 自动开仓评分门槛固定为 **3.5/10**，不再允许修改。
- 评分等级调整为：
  - **3.5–5.0：一级开仓信号**，本轮最多开仓 1 次，风险倍率 1.0×。
  - **5.5–7.0：二级强信号**，本轮最多追加 1 次，风险倍率 1.5×。
  - **7.5–10.0：三级超强信号**，本轮最多追加 2 次，风险倍率 2.0×。
- 每根新的已收盘 5m K 线最多只允许提交 1 笔开仓/加仓；评分下降不补低等级仓位。
- 做多与做空统一要求 **5m Trigger > 0**；Trigger=0 时禁止首仓和加仓。
- 完全平仓后，一级/二级/三级次数全部重置。

## TP / SL 变更

- 取消 TP1/TP2 分批止盈。
- 每一笔首仓或加仓都独立使用：
  - **SL = 1R**（继续由 15m ATR × 止损倍数确定）
  - **TP = 2R**
- 整仓在 2R 一次性止盈，不再执行首个 TP 后移动保本。
- 每笔限价开仓继续在 OKX 侧附带交易所原生 TP/SL；TP 与 SL 均为触发后市价执行。

## 加仓安全约束

- 同方向已有仓位时，只按评分等级升级规则追加，不允许普通重复开仓。
- 存在未成交加仓限价单时，不继续提交下一笔加仓。
- 每笔加仓必须先核对前一笔成交与 TP/SL 保护状态。
- 日内剩余风险预算会扣除当前已持仓预估风险；最大名义仓位按本轮总持仓控制，不允许每笔重复占满上限。
- 任何写入结果不确定、仓位数量不一致或 TP/SL 无法核实，仍按 fail-closed 处理。

## UI

- 执行参数页评分门槛显示为 **3.5（固定）** 并禁用编辑。
- 固定执行成本面板去掉外层黑色框/黑色边缘。
- 交易计划改为显示 **整仓 TP 2R / SL 1R**，不再显示分批止盈说明。
